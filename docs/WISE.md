# MCP Fleet Diagnosis (WISE)

Live diagnosis of every MCP server in the Claude Code config (`.mcp.json`, generated by
`scripts/mcp_write_local.ps1` from `~/.chthonic/api_pool.json`). Probed 2026-06-07 by
direct MCP handshake (initialize → tools/list). Tokens read from the pool, never printed.

The config was merged to parity with the VS Code stack (`.vscode/mcp.json`): the Claude
core 8 plus the 18 VS Code servers = 26 total. Translations: `${workspaceFolder}` →
`$repoRoot`, `bun`/`bunx`/`uv`/`uvx` → absolute paths, HF `${env:...}` → pool Bearer.

## Headline — GitHub MCP is lossless at 94 tools

VS Code reaches the Copilot-hosted GitHub MCP by OAuth (its log: "Discovered 94 tools").
Claude Code can't run that OAuth flow, so it sends a static `Bearer` token from the pool.
Proven equivalent by handshake against `https://api.githubcopilot.com/mcp/`:

| Headers | Tools |
|---|---|
| `Bearer` only (no toolset header) | 47 |
| `Bearer` + `X-MCP-Toolsets: all` | **94** |
| + `X-MCP-Insiders: true` | 94 (no new tools) |

`X-MCP-Toolsets: all` is the lever. It unlocks the 47 heavy tools: `actions_*` (CI),
code-scanning, dependabot alerts, security advisories, discussions, gists, notifications,
`projects_*`, secret-scanning, `semantic_issues_search`, `web_search`, repository tree,
support-docs search. The writer now emits that header, so Claude Code gets all 94.
`X-MCP-Insiders: true` adds zero tools today; kept for parity (may gate preview behavior).

Static Bearer + `X-MCP-Toolsets: all` == VS Code OAuth + same header. Lossless.

## Fleet table

| Server | Kind | Version / Protocol | Status | Modernize |
|---|---|---|---|---|
| github | hosted http | Copilot · 2025-06-18 | 94 tools (proven) | done — header added |
| huggingface | hosted http | v0.3.19 · 2025-06-18 | 9 tools, current | — |
| bun-docs | hosted http | v1.0.0 · 2025-06-18 | 2 tools, current | — |
| microsoft-docs | hosted http | v1.0.0 · 2025-06-18 | 3 tools, current | — |
| context7 | hosted http | v3.1.0 · 2025-06-18 | 2 tools, current | optional API key for higher rate limits |
| browser | bespoke · official SDK | bun-cdp (raw CDP), 7 tools | works, but SUPERSEDED | **modernize → `Bun.WebView`** — native headless, added Bun 1.3.12 (Chrome backend cross-platform, Playwright-style auto-wait). In-code "swap to @playwright/mcp" note is STALE. |
| filesystem | launcher · official server | `@mcp/server-filesystem` | current + self-healing file-URI patch | drop patch when upstream fixes `roots-utils` |
| pnk-archaeology | bespoke · FastMCP | repo-agnostic | current | — |
| pnk-public-archaeology | bespoke · FastMCP | repo-agnostic | current | — |
| chthonic-archaeology | bespoke · FastMCP | repo-agnostic | current | — |
| github-archaeology | bespoke · official SDK | 4 tools (triage/analyze/report + chronicle) | current | — |
| mas-mcp | bespoke · FastMCP | M-P-W router + GPU probe | current | ties to end-goal GPU lane |
| chthonic-v3 | bespoke · **Rust / rmcp 1.7** | stdio · 2025-06-18 | **ported** (was hand-rolled 2024-11-05) | done — `tools/chthonic-mcp-server` |
| game / sourcer / ssot / sonic / corpus | core (Claude lane) | active | maintained in other lanes | — |
| cocoindex-code | local bin (`ccc`) | — | current | — |
| workiq | bunx `@microsoft/workiq@latest` | — | current | — |
| sequential-thinking | bunx (official) | — | current | — |
| memory | bunx (official) | — | current | redundant with Claude's file memory — parity only |
| fetch | uvx (official) | — | current | overlaps built-in WebFetch |
| time | uvx (official) | — | current | — |
| git | uvx (official) | — | current | overlaps Bash git |

## Provenance & staleness (read before trusting a row)

Tool counts here are from **live enumeration** this session (initialize + tools/list), not
from each server's header comment — because in-code docs go stale. Two caught on review:

- `browser` carried an `@UpstreamReady` comment: "swap to @playwright/mcp when Bun IPC
  stabilizes." Obsolete — Bun shipped native `Bun.WebView` in v1.3.12 (running 1.3.14); that,
  not @playwright/mcp, is the modern target. mcp-browser.ts still uses the bun-cdp POC.
- `github-archaeology`'s header said "three tools"; it serves **four** (a `github_chronicle`
  tool was added). The header lied; the live list did not.

Lesson (logged): derive from the running surface, not the comment. Superseded docs/POCs get
quarantined under `confiscated_instructions/` (the repo's deprecated-doc staging area) rather
than left to mislead. Rows not re-verified live this
pass (e.g. whether the filesystem file-URI patch is still needed against current upstream)
are code-sourced — treat as "claimed, not proven" until checked.

## Modernization #1 (DONE) — chthonic-v3 → Rust

The bun `scripts/mcp-chthonic-server.ts` (v3.3.0) hand-rolled the JSON-RPC envelope on the
2024-11-05 protocol and exposed ~30 tools, ~half of them polyglot runners
(cargo/uv/gcc/go/ruby/git/bash/bun/make) already covered by the native Bash tool and the
`git` MCP server. It is now ported to Rust on **rmcp 1.7** — the official MCP SDK; the GitHub
Copilot SDK is client/agent-only and cannot build a server (the existing `tools/chthonic-mcp`
crate is a Copilot agent, not an MCP server). New crate: `tools/chthonic-mcp-server`.

The port keeps only the unique value — the `chthonic.ps1` domain/action router — exposed
intuitively as 6 tools: `chthonic` (generic `<domain> [action] [args] [-Json]` runner),
`chthonic_commands` (discover the surface), `chthonic_status`, `chthonic_doctor`,
`chthonic_ssot`, `chthonic_toolchain`. Redundant runners are dropped. Args pass as discrete
process arguments via `-File` (no shell interpolation — a safety upgrade over the old
`-Command` string).

`mcp_write_local.ps1` points `chthonic-v3` at `target/release/chthonic-mcp-server.exe` when
built, falling back to the bun server otherwise (no-delete — the old script stays as the
safety net). Build with `cargo build --release -p chthonic-mcp-server`.

## Redundancy — kept for parity, know the overlap

- `memory` (knowledge-graph MCP) vs. Claude Code's own file memory — different mechanism.
- `fetch` vs. built-in WebFetch; `git` vs. Bash git — MCP versions return structured calls.

These are harmless duplicates included for one-to-one parity with the VS Code set. Prune
later if the duplication is noise.

## Activation

`.mcp.json` is read at Claude Code startup, so changes land on the **next session**, not
the current one. Regenerate any time with `pwsh -NoProfile -File scripts/mcp_write_local.ps1`;
inspect with `-List` (prints the server table, no token values).

## Compound

- 94 GitHub tools make CI status, security/dependabot triage, projects, secret-scanning,
  and semantic/web search first-class — drive the noise lanes already in `manifest/` from
  inside the session instead of out-of-band scripts.
- `mas-mcp` GPU probe + the archaeology trio = device-local + cross-repo intelligence that
  feeds the end-goal CUDA/Vulkan compounding lane.
- Next compound: modernize (or fold) chthonic-v3, then let the unlocked GitHub + GPU
  surfaces drive the next unknown — compound the compound.
