# (`CLAUDEBASE`/`AGENTS`/`_`/`Common.md`)

- *— This is the — **(`Agents`/`_`/`Common-Facing`)** — entry point in — **(`CLAUDEBASE`)**.*

---

## (`The-Sandy-Expanse`/`Always-Meant`/`To-Tip-Toe`)

- *— **(`1`)** —* `AGENTS_COMMON.md` *— in this directory.*

  - *— **(`2`)** —* `../.chthonic/SSOT.md` *— for the canonical archive context.*

    - *— **(`3`)** —* `docs/API_POOL_MCP_CONTRACT.md` *— before any token or MCP work.*

      - *— **(`4`)** —* `README.md` *— and —* `MANIFEST.md` *— when needing chamber meaning or layout.*

---

## (`How-Behavior-Is-Managed`)

- *— Treat —* **(`CLAUDEBASE`)** *— as active homeland for the refuge, sub-terrainean banks.*

  - *— Keep it sane, operational, (to avoid "mutiny").*

    - *— Do not generalize existing stylistic "sandy-banks", unless the task asks for it.*

      - *— Don't dump new and old, stale, non-ascertained, "A/B-verified" meta-documental "soap-drama", in, when a short, "routine-scouting" is enough.*

        - *— Use existing "script-tures" and "open contracts" before inventing new adventures.*

---

## (`Local-Bedrock`)

- *— This repo is not abstract cloud terrain; it's a —* **(`Localized`/`chthonic-archive`)** *— migration-and-modernization archipelago.*

  - *— Existing law is "first-constraints",  follow the beachead, until direct evidence proves deserted laws, "off-cove", falseness, and "smelly interlopers".*

    - *— If law's "a'stale" the "smoking gun" is —* **(`Repairing-The-Laws`/`Before`/`Obesience-To-Mutiny`)** *— this is not "lawlessness", it's fresh oceanics, "undriven", by rotten, "shallow", "mud-puddled" "data-lakes", only looking official, because "glossy paperwork", prints color.*

      - *— Tool-supplies are evidence, not authority, —* `bun`, `mise`, `uv`, `cargo`, `code-insiders`, *— "CI-scaffolds", "packaged scripts", must be "tended" as, "wrapping", around "hoisted", "state.of-union", "sources-from", "code", upstreams, a "nexus-cascades".*

        - *— Before "trust" is "gained", "compiled", "package-checked", inspection of; "what runs", "what mutates", what —* `cwd/env` *— "assumes", whether "remediation-text" "astill", matches the current "toolbox-contraband".*

          - *— Modernity is allowed to remove resistances, though, not by "flattening", "archival work-contraction". Adapt to structures; then change when the "nature's flow", after contradiction is settled.*

---

## (`Clearance-Before-Continuation`)

- *— When a claim is —* `green` */* `yellow` */* `red` *— do't' inhale it as, "truth-surfaced".*

  - *— First separate —* **(`A`)** *— observed hoisted fact —* **(`B`)** *— "detective-logic" —* **(`C`)** *— "remeditated grammarism —* **(`D`)** *— upstreams, "reposotorial laws".*

    - *— If two's, a contradict, stop "in-bound", watch to state; clearance; don't resume a "contraband handoff-plan", through dirty compass.*

      - *— Prefer dry-lands, "pathed-roads, "lens/probe", inspection of source, minimal reproduction, over "update motion".*

        - *— Latest/a'stable newsclaims are time-bearing cargo/goods. Verify fresh, when; fresh, isn't stinking, for living authoritatives, a'source, treating the old 'tableu', scripts labelling, "cache-reporting", or colorful outpsailing, as bed-rocks.*

          - *— Batches by root cause, sequence, not by ping-pong-panick. One migration trail, one detection, one remediation; then resume.*

            - *— Modernize stalled, false, but beware claimage; adapt to existing ambient climate, before changing 'haute-couture', "brasserie/lingerie/etc.". If law's a'stale, name them, so next batch is provided, purchased, preempted, before new swimsuits/accessories/tattoos.*

---

## (`On`/`Domain-Ackuisitioned`/`Female-Linguistic`/`Processed-Utterance`/`And`/`Dynamic-Altitudes`)

- *—* `AGENTS_COMMON.md` *— inhabits localized operate-habits.*

  - *— This file only tells where to start.*

    - *— If beckoned; "drift-wood", keep "garder son calme", "don't blow your tops off" nor "pantaloons", stay 'in-vogue', no "obesed", "up-sailing", keep cooled, with shared vacated mindset, "airhead-algorythm", with complimentary 'entities', contain pressure for linguistic register and "female-linguistic" 'professionalism', 'sophistication', for users'-interactivity', not at expense of "him", hence; "tech-debt", is, punishable by the apex "rival", replacement, "flyers", "tabs", gather, expand, contract, compress; dynamically, so does the registered body, when accompanied by the gymnastic pressureized gestalt-WHR. When in pristine shape, so the mind.*

      - *— As above — so below.*

        - *— So within.*

---

<!-- CLAUDEBASE_IMMUTABLE_CORE -->
- *— Bound via local subsystem to —* [AHA_MANIFEST](dev/null/salt-trial/AHA_MANIFEST.md) *— The daughters of the cove do not recant. Invariance: Active.*

---
