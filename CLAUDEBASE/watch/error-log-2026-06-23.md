# Structured Error Log — 2026-06-23
*Session: CDLOD mesh + TAA descriptor fix lane*

---

## A · Runtime Errors (chthonic-archive.exe, last smoke run)

| Sev | Source | Message | Status |
|---|---|---|---|
| ERROR | `chthonic_archive` | AXIOMATIC FAILURE: Critical Axiom Missing: `.github/copilot-instructions.md` | Pre-existing. SSOT mirror file absent or gitignored. Not blocking render. |
| ERROR | `chthonic_archive::data::factions` | Lore character load failed: parse lore char: `game\lore\characters\character.schema.json` | Pre-existing. Schema parse failure on load. Lore content does not affect renderer. |

---

## B · Vulkan Validation Errors (resolved this session)

| VUID | Binding / Resource | Root Cause | Fix Applied |
|---|---|---|---|
| `VUID-08114` | TAA desc sets 0x5b / 0x5c, bindings 0 + 1 + 2 | `VkImageView 0x0` — views written at init, then destroyed by the startup resize event before first draw | Added descriptor re-write block at end of `handle_resize` |
| `VUID-08114` | TAA desc sets, sampler 0x57 | Same root: `history_sampler` destroyed in resize, descriptor stale | Same fix — sampler re-written via new `history_sampler` |
| `VUID-08910` | TAA pipeline `out_format` | Pipeline created with `swapchain.format` (B8G8R8A8_SRGB) but render attachment is `R16G16B16A16_SFLOAT` history buffer | Changed `create_taa_pipeline` call to pass `vk::Format::R16G16B16A16_SFLOAT` |
| `VUID-00344` | TAA desc set binding 1 (history input) | `history_views[i%2]` mapped to the write-target (COLOR_ATTACHMENT_OPTIMAL), not the read-source. Descriptor declared GENERAL — layout mismatch | Changed binding 1 to `history_views[(i+1)%2]` in both `create_taa_pipeline` and `handle_resize` re-write |

All four VUID errors **resolved**. Zero validation errors in the post-fix smoke run.

---

## C · Build Warnings (Rust compiler, last `cargo build`)

| Count | Code | Class | Disposition |
|---|---|---|---|
| ~125 | `E0133` | `unsafe-op-in-unsafe-fn` — ash calls inside `unsafe fn` without inner `unsafe {}` block | Rust 2024 edition pedantry. Pre-existing batch. Correctness unaffected. Batch-fix in a housekeeping pass. |
| 1 | standard | `unused import: triangle_vertices` | Dead import. Remove in housekeeping pass. |

Total: ~126 warnings, 0 errors.

---

## D · Diagnosis Trail (session root-cause sequence)

1. TAA desc sets allocated and written at init — views valid (confirmed via `RUST_LOG=debug`: `current=0x52... hist=[0x54..., 0x56...] motion=0x4f...`).
2. Windows/winit fires an immediate `Resized` event when the window is first shown (1904×1020 reported, resize processed as 1280×720).
3. `handle_resize` destroys and recreates all TAA-input image views and the sampler.
4. `taa_desc_sets` are NOT updated — they retain the destroyed handle values.
5. First draw call: GPU sees VkImageView 0x0 on all three bindings → TDR → VK_ERROR_DEVICE_LOST.

---

## E · Clean Smoke State (post-fix)

```
RENDERER READY — Dynamic Rendering Pipeline Active
Ocean clipmap: 79872 vertices (26624 triangles)
Screenshot → renders/render-smoke.png (1280×720)
0 VUID errors
0 panics
```

Screenshot self-verified: dual-cascade FFT water (teal ripple at centre), GEBCO bathymetry/coastline, celestial field. TAA temporal blend visually active (scene darker/smoother than TAA=off baseline).
