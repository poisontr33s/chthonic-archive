# Frontier Atlas — the cross-referenced gap map (2026-07-07)

Built at the user's request for a "kartlegging med referanser" across everything in-house and from-scratch, to separate what's genuinely alchemizable right now from what only looks that way. Every entry below cites the memory file or repo path it's grounded in — no claim here should be trusted further than that citation goes. Two entries were caught actively drifted from their own source during this pass (noted inline) and corrected on the spot, which is itself the method: the atlas is only as good as its citations stay current.

Categories are ordered by what a session should reach for first, not by project size.

---

## Level map — clusters and nearest neighbors

The categorical sections below (0-5) are the surface. This is the foundation underneath: which projects actually sit near each other, by shared file, shared substrate, or shared method — not by shared vibe. Built from what already existed across memory + the north-star chart, not invented fresh.

**Level 0 — the thread.** Not a project; the law running through all of them, named explicitly in `[[project_aca_engine_philosophy]]`: *is it forced by a substrate you can check, or a free parameter wearing the costume of truth?* Same law shows up as "standard-as-floor, distinctive-as-layer" in the renderer's guardrails, as "surface=creative/substrate=verifiable" in the CLAUDEBASE frontmatter standard, and as the rewindability discipline in the DSL nightly. Three domains, one law — this is the thing that makes the clusters below legible as one map instead of five unrelated lists.

**Level 1 — clusters, by nearest neighbor:**

| Cluster | Members | Shared substrate | Nearest other cluster |
|---|---|---|---|
| **Renderer/Vulkan core** | Rung 2.5 RT reflections, atmosphere UBO/ray-sphere bug, A-C-A live correspondence, North Star checkpoint, Fork-III ocean BLAS | `renderer.rs`, plus this session's own method (numeric/instrumented verification over eyeballing a screenshot) | DSL (shares the rewindability/cessation discipline — the A-C-A nightly cited the DSL nightly's L45 fork by name) |
| **GPU/ML compute** | FLUX C++ TRT pivot, FLUX modernization plan, FLUX sysmem fallback, NVIDIA stack inventory, TRT cp314 gap, chthonic-hw-mcp-server, tabby-modern G10 | CUDA/TensorRT/driver stack, cross-checked via `hw_inspect`/`hw_drift_check` | Toolchain/environment (version-manager discipline is the same shape, GPU driver vs. language runtime) |
| **DSL/text-substrate** | DSL substrate methodology, DSL iteration rewindability, lore-canon directory structure, CI lore-canon membrane stack | `tools/dsl-smoke`, `.chthonic/grammar/chthonic.peg`, `.chthonic/SSOT.md` | Renderer/Vulkan (shared method, not shared file) |
| **Data-archaeology** | Zombie evolution, corpus boomerang/gitological lenses, session-vampire/session-corpus tooling | Tightened — these don't share one substrate: zombie evolution owns `dumpster-dive/intake/` (its `.pkl` models, slag manifests) and never touches `corpus.sqlite`; corpus boomerang/gitological lenses/session-vampire all center on `corpus.sqlite`'s `session_ranked` pipeline and never touch `dumpster-dive/`. Clustered by shared *method* (archaeology over accumulated artifacts), not a shared file | DSL (both are text/corpus processing, but different intake — SSOT.md specifically vs. broad dumpster intake) |
| **Toolchain/environment** | Ruby 4.0.5, R lang, VS 2026 variants, Mise slab, Rust toolchain modernization | polyglot version-manager convention (`uv`/`rv`/`goup`/`rig`/`rustup`) | GPU/ML (NVIDIA stack inventory sits on both sides) |
| **Governance/hygiene** | PKFH Sentry incident (closed), CI autofix gate architecture, SID/envelope standards | repo-wide CI, not a "frontier" so much as the floor everything else stands on | touches all clusters peripherally, isn't itself one |

**What this actually buys you, now that it exists:** picking a session's work by *cluster* rather than by single project means the verification method, the file layout, and the open forks from the last thing done in that cluster are still warm — tonight's A-C-A work reused the DSL nightly's exact discipline because they're nearest neighbors, not because I went looking for a precedent. Next time you're choosing what's next, "what cluster am I already warm in" is a cheap, real signal — cheaper than re-deriving from the categorical list alone.

---

## 1. Ready to alchemize now (gate already met)

- **Zombie evolution — Tier B fully shipped, 2026-07-08 (B2 in the evening session, B3 overnight via `/nightly`).** Corrected twice earlier the same session (A4-stale, then B1-also-wrong — see `[[project_zombie_evolution]]` for the full account) before landing on the real gap: B2, provenance features into the GBT model. Built `_git_provenance_features(path)` (git log --follow, survives the excrete git-mv) feeding `days_since_last_touch`/`num_prior_edits`/`is_orphaned` into a 10-feature model; `_ml_ore_rating()` gained a 10/7/5-feature graceful-degradation branch so older pickled bundles keep working pre-retrain. Deliberately partial and said so in the code: `is_orphaned` is git-activity-only, since `import_graph` is a co-occurrence graph, not a reverse-dependency index, and can't answer "no downstream imports." Verified against the real pickled bundle (`clf.n_features_in_ == 10`), not the display panel (which had its own unrelated stale hardcoded feature list, fixed in passing); `bite` fires a live `ore_ml` prediction from the new model; B1/`embalm_provenance` and `upcycle` both unregressed. Honest number: CV accuracy 84.4% → 83.8%, a slight dip on a small sparse-class dataset, not oversold as a win.
  **B3 (enriched forge-bridge receipts), same night, via the first real `/nightly` invocation:** verify-before-assume applied one more time and found a genuine middle case — the bridge already had a B1-era `provenance` sub-object in every receipt, but B2's three git-derived fields never reached it (computed only transiently inside `bite()` for ML scoring), and `blame_author` existed nowhere. Extended `_git_provenance_features()` to also return `blame_author` off the same git subprocess call (widened format string, no second call); `_build_bride_provenance()` merges all four fields into the extract; the bridge's `_provenance_payload()` translates them to the plan's named receipt fields (`age_days`/`is_orphaned`/`blame_author`). Verified against real data: ran `chew` on a real file, cross-checked the new fields directly against raw `git log` (not the tool's own claim), fed the real provenance block through the actual translation function. B1/B2 both re-checked and unregressed. Two side findings named, not fixed: `chew --json` leaks a status line onto stdout ahead of the JSON (pre-existing, out of scope); and — larger — this entire session's accumulated work across many unrelated areas turned out to be sitting uncommitted in the tree, surfaced while sanity-checking git-history math, named for the user rather than batch-committed autonomously.
  **C1 (`zombie intake-report`), later the same night, the SECOND `/nightly` invocation — the first genuinely unattended one (user actually asleep):** grepped first, confirmed genuinely unstarted (unlike every prior tier's "already partially done" surprise). Built all four report sections the plan named; three reuse `_collect_training_data()` directly, only ML confidence (`predict_proba()`, additive) and semantic clustering (`networkx` connected-components) are genuinely new code. Verification caught two real issues before calling it done: a misleading `"communities": 1` top-line (was actually the unknown bucket — `community_map.membership` is completely empty right now, import graph under Louvain's 3-node floor) and a vague "too small or no matches" hedge on semantic clusters (the index is verifiably 0 entries, not just small) — both fixed with precise, honest wording rather than shipped as-is. A third independent check (manual `predict_proba` reconstruction) found no issue, confirming the mechanism itself is sound. `docs/zombie/CONVERGENCE_PLAN.md`, `UPGRADE_LOG.md`, memory, and `MEMORY.md` all updated; landing docs at `claude/mailbox/SESSION_2026_07_08_ZOMBIE_B3_AUTONOMOUS_NIGHT.md` and `SESSION_2026_07_08_ZOMBIE_C1_AUTONOMOUS_NIGHT.md`. Next is Tier C2 (upcycle propagation), pre-condition now met — verify-before-assume applies there too. Ref: `[[project_zombie_evolution]]`.
  **Zombie's own focused home is `docs/zombie/` — `CONVERGENCE_PLAN.md` (status + tier plan), `UPGRADE_LOG.md` (per-upgrade build record, now includes B1 retroactively + B2), `README.md` (cold-start/command reference).** `claude/mailbox/PENTEA_ROULETTE_STEWARDESS.md` also tracks zombie (its D3 table), but it's a shared ledger spanning five unrelated domains (ruby-zjit, REM/ankh-forge, governance, MILF-Core, autoloop infra) — useful for confirming a date once, not a place to keep reading for zombie specifically. Three files, one project, no cross-project noise.
- **Atmosphere UBO + ray-sphere bug — closed this session.** Not a forward-looking gap, but worth logging as freshly alchemized: `rayIntersectSphere`'s far/near-root bug (all 3 atmosphere shaders) plus the UBO offset bug, root-caused via a faithful numeric Python reproduction rather than blind edit-and-eyeball, fixed, render-verified via `CHTHONIC_LENS=perspective`. Ref: `[[reference_atmosphere_ubo_offset_bug]]`.
- **AxiomVerifier — up-cycled this session.** Hash moved from a hardcoded `main.rs` literal to a sidecar seal file (`.chthonic/SSOT.md.sha256`); doc comment's false "& SSOT Sync" claim removed. Ceremonial tone kept intact. Both this and the atmosphere fix are already committed (see §5).

## 2. Blocked on something outside our control

- **TRT cp314 gap.** `tensorrt`/`modelopt` deferred to the `[trt]` pyproject extra — zero cp314 wheels exist upstream. Not actionable until NVIDIA ships them; bf16 stays the default in the interim. Re-check wheel availability before spending a session assuming this has changed. Ref: `[[project_trt_cp314_gap]]`.
- **Ankhological Origin — zodiac meanings.** `src/render/zodiac.rs` has the Sirius/Alcyone midpoint ayanamsa (Option C) locked in; the `meanings` field is deliberately left empty. This is a creative-authorship gap, not an engineering one — no amount of session time closes it, only you writing the meanings does. Ref: `[[project_ankhological_origin]]`.
- **Fork-III — displaced ocean BLAS. Moved here from §3 — re-scoped, not re-gated.** Today's BLAS is built from the flat rest-state ocean mesh (all wave displacement happens GPU-side in `water.vert`), so ray-traced ocean self-reflection is cull-masked out entirely rather than rendered wrong. Re-examined the actual code rather than trusting this entry's own prior write-up (per the pre-CLAUDEBASE-drift lens, §6): the hard part — simulating the waves — is already done and running every frame. `water.vert` sources displacement by sampling two existing GPU textures (`u_displacement_c0`/`c1`, RGBA16F, one per Tessendorf cascade) that `ocean_compute.rs`'s FFT pipeline already writes. What's actually missing is a compute pass reading that already-existing data and writing it into a BLAS-shaped position buffer, then an incremental `VK_BUILD_ACCELERATION_STRUCTURE_UPDATE` (Vulkan's standard, well-documented refit path) rather than a full rebuild — `ray_tracing.rs`'s own comment already calls this "a localized change." That's real work, but not from-scratch/foundational-tier — it compounds on an existing compute pipeline and a standard Vulkan RT pattern, so §3 ("needs foundational/from-scratch work") was the wrong home for it on technical grounds. It belongs here instead: the actual blocker was never engineering difficulty, it's the user's own "lived experience with the water in motion" call — an experiential readiness question outside engineering's control, same shape as this section's other two items. Correcting the engineering assessment doesn't mean the gate has been met; that's still explicitly theirs to open. Ref: `CLAUDEBASE/charts/north-star-constellations.md` §4.5.

## 3. Needs foundational/from-scratch work — the actual slugger territory

- **FLUX C++ TRT pivot.** Approved plan: honesty pass → FP8 gate → collapse the Python glue → native Rust/C++ bridge → eventually Vulkan-side. Engine is still bf16 today; the DiT FP8 work happens in a native TensorRT SDK bridge, with the satellite process owning only CLIP-L + T5 + VAE. This is the single biggest from-scratch engineering lift on the board — real C++/TensorRT SDK work, not a refactor. Ref: `[[project_flux_cpp_trt_pivot]]`, `[[project_flux_modernization_plan]]`, `[[reference_flux_sysmem_fallback]]` (the bf16 22.7GB/24GB WDDM-spill root cause that makes FP8 not just a speed win but a stability fix).
- **DSL substrate (Ankh-DSL) — Phase 2 compile, NO-GO, still standing.** `docs(chart): re-scope` and this session's own observed Codex-lane commit (`bc7fd02b`, 11:59:56 today — "Refactor sonic_window manifest... NO-GO status for Phase 2, new coverage surfaces: bold/backtick/fenced") confirms this thread is live and being actively tended by the autonomous lane, not abandoned. Phase 0 grammar extraction → Rust parser → AST/interpreter → translation boundary is the standing methodology; Phase 2 (actual DSL→something compile) is the documented blocker. Ref: `[[reference_dsl_substrate_methodology]]`, `[[reference_dsl_iteration_rewindability]]`.

## 4. Recently closed — update the mental model

- **Rung 2.5 (Phases 0-5), full RT-reflections pipeline** — done, reviewed, pixel-diff verified (13,594/921,600 differing pixels, 1.48%, bounded to shallow water, zero in deep water). Ref: `[[project_rung_25_rt_reflections_checkpoint]]`.
- **Checkerboard artifact + Copernicus bathymetry promotion** — closed prior session.
- **A-C-A engine — live correspondence.** Overnight prototype: the tested/JPL-verified triad (zodiac.rs + cosmos.rs) was wired to a frozen verification epoch and a write-only struct field nothing read — the engine's own "forced vs ornamental" criterion, applied to its own integration, said ornamental. Fixed: `cosmos::julian_day_now()` added and wired into the correspondence-socket reading only, celestial-field mesh left pinned for the smoke-test pixel baseline (verified byte-identical). Per-frame recompute left as an explicit open fork, not decided autonomously. Ref: `[[reference_aca_engine_live_correspondence]]`.
- **Camera input wiring — CLOSED, user-confirmed hands-on.** `main.rs` had zero keyboard/mouse handling; `camera.rs`'s `set_distance`/`set_target` sat dead. Wired WASD (screen-relative continuous pan via new `ground_axes()`) and mouse wheel (new `zoom()` method — caught that `set_distance` alone is a no-op under this camera's orthographic projection; `ortho_size` is the real control). Pixel-diff proved the render math end-to-end (pan/zoom both moved 60-65%+ of sampled pixels, frame-wide) before the user tested it live and confirmed: "the WASD works." Nothing left open on input wiring itself. Ref: `[[reference_camera_input_wiring]]`.
- **TAA ghosting under camera motion — CLOSED, verified.** Three-pass investigation same session. First hypothesis (jitter-only motion vectors) checked against real code and found wrong; real mechanism found (`blend: 0.1`, `renderer.rs:2725`, hardcoded, never motion-adaptive); attempt 1 (raw per-pixel motion threshold) regressed wave-crest streaking, reverted clean; a sustained-motion diagnostic (`CHTHONIC_TEST_SUSTAINED_PAN` + configurable `CHTHONIC_SCREENSHOT_FRAME`) then proved the artifact was an unbounded, ever-growing streak, not a bounded error. Attempt 2 fixed it: a camera-only motion scalar, computed CPU-side by projecting the camera's own `target` through both `current_view_projection`/`previous_view_projection` — same point both times means the delta is pure camera motion, since wave animation can't move the camera's target. Wired in as a 5th `TaaParams` push-constant field (16→20 bytes, same growth shape as Fork-V's own push constant), driving `adaptive_blend` in `taa_resolve.frag`. Verified: static baseline byte-identical (152021 bytes, zero regression); sustained-pan frames 5/20/60 re-captured and read directly — clean at all three, where frame 60 previously ran the streak off both edges; one-shot zoom diagnostic shows no new artifact. Ref: `[[reference_taa_ghosting_investigation]]`.
- **Fork-V — Beer-Lambert hit-shading for RT reflections.** Picked as the one thing to take to its best form when given an open choice across the whole repo. `water_reflection.rchit` now ports water.frag's real depth-driven Beer-Lambert seabed model (exposed-cay per-vertex color, underwater fixed-SAND-reflectance + water-column in-scatter, same constants/citations) instead of flat vertex-color × N·L — deliberately without the hero shader's final surface Fresnel/sky blend, since a single-bounce hit shader shouldn't layer a second reflection on the first. Sun intensity reached the hit shader via a widened RT-local push-constant range (16→20 bytes), not a resize of the shared `FrameUniform` UBO — smaller blast radius, this session's own atmosphere-bug lesson applied. `cargo test` 51/51, clean `render-smoke`, and this time a real pixel delta (152021 vs 163308 bytes) since it's the one Rung 2.5 fork that changes reflection appearance, not just correctness. Ref: `CLAUDEBASE/charts/north-star-constellations.md` §4.5, `[[project_rung_25_rt_reflections_checkpoint]]`.
- **Non-disruptive verification + shader tooling — CLOSED, verified.** Three-part pass (`linear-mapping-rain.md` Meta-track M): (1) `CHTHONIC_HEADLESS=1` parks the render window off-screen instead of hiding it — `.with_visible(false)` was tried first and found (empirically, not assumed) to silently stop winit from ever delivering `RedrawRequested`, stalling the whole loop; off-screen positioning keeps the redraw loop alive while nothing appears on any real monitor, now `render-smoke.ps1`'s default. A second miss caught by the user hands-on: the off-screen fix only addressed the graphics window — an empty console window was still popping up separately (Windows auto-allocates one for any console-subsystem binary regardless of stdout/stderr redirection); fixed with `kernel32::FreeConsole()` gated behind the same flag, verified via a real Win32 `EnumWindows` probe against the live process (no console-classed window present, only the graphics window and winit's own normal internal event-target window). (2) A 5-repo reference-code scouting pass (real function-level reads, one wrong hypothesis self-corrected) shipped `ci/checks/glsl-lint.ts` (glslangValidator wrapper, registered in `ci/run.ts`) and closed a `build.rs` backend-parity gap (`shaderc`-crate fallback now has `set_include_callback`, matching `glslc`'s already-working native `#include`) — both verified against real compiles, not just "doesn't crash." (3) Root `README.md` written from scratch — didn't exist before. `cargo test` 55/55 throughout. Ref: agent transcript in Meta-track M2, `CLAUDEBASE/charts/north-star-constellations.md` §7.
- **`NEW_PROVIDENCE_LAT_DEG`/`LON_DEG` coordinate drift — found and fixed.** `cosmos.rs` and `geodesy.rs` each independently declared this project's fixed Nassau reference point, with different values (25.0443/-77.3504 vs 25.0489/-77.3555, ~500m apart) — meaning the JPL-Horizons-verified sun-position math and the bathymetry/ENU terrain anchor were silently computed against two different physical points. Found while searching `src/` for old/misleading code per the user's own direction, after `vulkan-lab/` (a genuinely active, non-abandoned external R&D lab building RTX 4090 cooperative-matrix/tensor-core GEMM — proven since April, never integrated into this engine, no clean current fit since none of this engine's compute subsystems are matrix-multiply-shaped) turned out not to be the intended target. Fixed by consolidating on the JPL-verified value (`cosmos.rs`'s own test suite anchors its expected sun angles to 25.0443/-77.3504 via its own hardcoded test-local constants, decoupled from the public one, so this was safe to redirect without touching test fixtures): `geodesy.rs` now carries the correct value as sole canonical source; `cosmos.rs` re-exports rather than redeclaring. `cargo test` 55/55 (all JPL/Skyfield-anchored cosmos tests + all geodesy round-trip tests unaffected); `render-smoke` clean, expected small pixel delta from the ENU origin actually shifting ~500m (152021→151424 bytes), visually confirmed healthy.

## 5. Dormant/stable — no action needed, don't spend a session here

- VS 2026 variants (all four kept, `.vsconfig` exported) — `[[project_vs_2026_variants]]`
- Ruby 4.0.5 lane, R lang 4.5.3 (rig + rv-r) — `[[project_ruby_404_lane]]`, `[[project_r_lang_broken_install_2026_07_03]]`
- Mise slab (all 12 tasks verified) — `[[project_mise_slab_monorepo_wiring]]`
- NVIDIA stack — **corrected during this pass**: the 2026-06-16 snapshot said Streamline/DLSS "NOT installed," but the live `chthonic-hw` MCP snapshot today shows DLSS 310.7.0.0 present and `streamline_ffi.rs`'s DLAA bridge is actively shipped in the renderer (this session's Phase 5 work sits right next to the DLAA resolve branch). It was installed sometime in the last 3 weeks and the memory hadn't caught up. Ref: `[[project_nvidia_stack_inventory]]`, live via `hw_inspect`/`hw_drift_check`.
- chthonic-hw-mcp-server — built, E2E verified, is now the live source of truth superseding the static NVIDIA inventory above.

## 6. Pre-CLAUDEBASE temporal anchor — staleness triage heuristic

**Anchor date: 2026-06-06** — CLAUDEBASE's first commit (`git log --diff-filter=A -- CLAUDEBASE/`, verified, not guessed). Content dated or last-touched before this line is more likely a historical monument than live work; content from this date forward is CLAUDEBASE-era and more likely actively maintained. Neither claim is automatic — verify before trusting either way — but it's a real, checkable starting heuristic rather than a guess from vibes.

This section exists because the repo has genuinely accumulated dozens of top-level directories over many months (`ls -d */` at repo root runs to 60+), and re-discovering from scratch each session which ones are current vs. historical is exactly the ping-pong the responsible-work-order discipline exists to prevent. **Add a row here whenever a top-level directory or major subsystem gets triaged — don't re-investigate what's already anchored.**

| Directory/area | Last real activity | Verdict | Notes |
|---|---|---|---|
| `vulkan-lab/` | 2026-07-03/04 | **Active, deliberately separate** — not stale | RTX 4090 cooperative-matrix/tensor-core R&D; "C++ explores, Rust hardens" methodology; proven GEMM work (April) never integrated into `src/`, no clean current fit since none of this engine's compute subsystems are matrix-multiply-shaped |
| `src/render/geodesy.rs` + `cosmos.rs` | tonight | **Fixed** — was live drift, not staleness | Two independent `NEW_PROVIDENCE_LAT_DEG`/`LON_DEG` declarations, ~500m apart; consolidated on the JPL-Horizons-verified value |
| `chthonic-golden/` | 2026-02-26 | Pre-CLAUDEBASE, no recent activity — **unclassified** | VS Code fork/rebrand scaffold (`product.json`/`quality.json`/Electron); unrelated to the renderer on its face; not yet investigated for current relevance |
| `chthonic-vscode-extension/` | mixed (Dec 31 core, Jul 4 docs) | **Ambiguous — unclassified** | Separate from the real `extensions/chthonic-archive/`; some files recently touched, needs its own look before any verdict |
| `chthonic-archive/` (nested Python project) | 2026-05-09/25 | Pre-CLAUDEBASE — **flagged, not investigated** | Unrelated Python/Docker agent scaffold nested inside this repo's own root; contains a `.env` file — worth a closer look for that reason alone |

Surveyed but not yet triaged (from tonight's top-level `ls -d */`): `The-Savant-Grade-Undercellar_Library_Study/`, `WET_PAPER_TO_GOLD_WIP/`, `anti-patterns/`, `birdcage/`, `bun-playwright-poc/`, `csb-live/`, `eoai-live/`, `git-dump-live/`, `pnk-lfh-live/`, `pnk-live/`, `poisontr33s-live/`, `rmco-live/`, `tmp-cai-test/`. The `-live`-suffixed ones look like they may be mirrors/mounts of the other repos in this session's own additional-working-directories list (poisontr33s, PsychoNoir-Kontrapunkt) — not confirmed, don't assume.

---

## The pattern underneath all of it

Every category above resolves to the same method already load-bearing in `north-star-constellations.md`'s guardrails: **standard-as-floor, distinctive-as-layer** — take the broadest cross-vendor/upstream-available baseline as the mandatory floor, keep the in-house work as an optional layer on top, never a replacement of the floor. And **up-cycle, don't delete** — the AxiomVerifier and the atmosphere shaders this session weren't rewritten from nothing; the ceremonial tone and the correct halves of the math both survived, only the actual defect moved. Section 3's three items are the only ones that don't fit that shape yet, because there's no existing floor to stand on — that's what makes them foundational rather than alchemizable, and why they cost more than a session.
