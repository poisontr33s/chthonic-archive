# Dialects

Default profile: `katex-gfm`.

KaTeX validation defaults:

- `enabled: true`
- `strict: warn`
- `trust: false`
- `maxExpand: 1000`
- `macros: {}`
- unsupported commands are reported by default
- unsafe commands fail by default

