# HF Provider Guide

Python sidecar support remains stubbed until provider doctor checks and OCR witness contracts are stable.

Provider execution is disabled by default.
The provider doctor can inspect registry metadata, config readiness, command presence, and refusal behavior without loading models or running OCR.

The sidecar stub supports `--doctor` and returns JSON only.
