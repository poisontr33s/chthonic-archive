# (`CLAUDEBASE`/`Codex.md`)

- *— This is the — **(`Codex-Facing`)** — entry point in — **(`CLAUDEBASE`)**.*

---

## (`The-Sandy-Expanse-Always-Meant-To-Tip-Toe`)

- *— **(`1`)** —* `AGENTS.md` *— in this directory.*

  - *— **(`2`)** —* `../.chthonic/SSOT.md` *— for the canonical archive context.*

    - *— **(`3`)** —* `docs/API_POOL_MCP_CONTRACT.md` *— before any token or MCP work.*

      - *— **(`4`)** —* `README.md` and `MANIFEST.md` *— when you need chamber meaning or layout.*

---

## (`How-Behavior-Is-Managed`)

- *— Treat — **(`CLAUDEBASE`)** — as active homeland for the refuge, sub-terrainean banks.*

  - *— Keep it sane, operational, (to avoid "mutiny").*

    - *— Do not generalize away the existing style unless the task asks for it.*

      - *— Do not dump new meta-docs into the tree when a short routing note is enough.*

        - *— Use the repo's existing scripts and contracts before inventing new wiring.*

---

## (`Win11-Local-Bedrock`)

- *— This repo is not abstract cloud terrain; it is a local — **(`Windows-11`/`chthonic-archive`)** — migration-and-modernization body.*

  - *— The existing repo law is the first constraint. Follow it until direct evidence proves the law stale, false, or incomplete.*

    - *— If a law is stale, the batch is — **(`Repair-The-Law`/`Before`/`Obey-The-Lie`)** — No modernization should be driven by a rotten source that only looks official because it prints color.*

      - *— Tool output is evidence, not authority, —* `bun`, `mise`, `uv`, `cargo`, `code-insiders`, *— CI gates, and package scripts must be read as wrappers around real host state, source code, and upstream drift.*

        - *— Before trusting a compiler/package/check lane: inspect what it actually runs, what it mutates, what cwd/env it assumes, and whether its remediation text still matches the current toolchain.*

          - *— Modernization is allowed to remove resistance, but not by flattening the archive's working contracts. Adapt through the current structure; then change the structure only when the contradiction is proven.*

---

## (`Clearance-Before-Continuation`)

- *— When a lane claims —* `green` */* `yellow` */* `red` *— do not inhale the color as truth.*

  - *— First separate — **(`A`)** — observed host fact, — **(`B`)** — detector logic, — **(`C`)** — remediation text, and — **(`D`)** — current upstream/repo law.*

    - *— If any two contradict each other, stop in clearance state; do not resume the handoff plan through a dirty compass.*

      - *— Prefer dry-run, path/version probes, source inspection, and minimal repro over installer/update motion.*

        - *— Latest/stable claims are time-bearing cargo. Verify them against the live authoritative source before treating the repo's old table, script label, cached report, or colorized output as bedrock.*

          - *— Batch by root cause and sequence, not by panic-ping-pong. One migration lane, one detector lane, one remediation lane; then resume.*

            - *— Modernize what is stalled or false, but adapt to the existing repo laws before changing them. If the law is stale, name that as the batch before enforcing it.*

---

## (`Domain-Expertistic-Aktitude`)

- *— `AGENTS.md` — inhabits local operating habits.*

  - *— This file only tells Codex where to start.*

    - *— If this becomes "drift-wood", keep "composure", "shorties", brasserie, and stay in shape, no "obese" "up-sailing", keep it cool with shared vacation with complimentary entities, have fun, not at the expense of "the user", hence; "tech-debt", is, punishable by the rival replacement, "contracts" gather, expand, comtract,  dynamicly, so does the body when in pristinee shape, so the mind.*

      - *— As above — so below.*

        - *— So within.*

---
