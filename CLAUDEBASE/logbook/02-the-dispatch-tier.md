---
- What-She-Wrote-Down: #!/usr/bin/env markdown
- SID: CLAUDEBASE_LOGBOOK_V1
- Claudebase-Flavored-Blend: permanently-living-document
- Ssot-Monolith: [ssot](../../.chthonic/SSOT.md)
- Open-Seas: chthonic-archive/CLAUDEBASE/logbook/02-the-dispatch-tier.md
- Entries: 3 · filled-last · by-creed
- Altitude: Captain's-Cabin · Amidships
- Island: Eleuthera · 25.1500,-76.1500 — oldest settlement, the record
- Real-Sky: --live (Open-Meteo; never stamped)
- Heat-Index: Trade-Wind · Languid · Contraband-Warm
- Cosmological-Altitude: --live celestial over this chamber's island · CLAUDEBASE_COSMOS_V1 · verified vs JPL Horizons
- Register-Blend: Nautical · Victorian · Renaissance · Carribbean
---

# (`☥`/`CLAUDEBASE`/`—`/`LOGBOOK`/`·`/`Entry-02`)

> *Master spikres før seil heises; et umerket ror adlyder hvem som helst.*

  > *Pin the masts before the sails are raised; an unmarked wheel obeys anyone.*

---

## (`Entry-02`/`·`/`2026-06-10`/`The-Dispatch-Tier-Was-Commissioned`)

- *— A fixed dispatch tier was berthed beneath the Opus 4.8 orchestrator, on commission from the —* `The-Savant-High-Bounties` *— register: two project-scoped subagents in —* [`../../.claude/agents/`](../../.claude/agents/)*.*

  - *—* **(`quartermaster`)** *— the hands. — `model: claude-sonnet-4-6` — (bare, never the alias, never **`[1m]`** —* `effort: max` *—* `memory: project`  *—  draws the subscription's *separate Sonnet quota lane; routine, well-scoped execution — implementation, refactors, tests, doc sweeps, satellite chores.*

    - *— **(`sailing-master`)** — deep water. —* `model: claude-fable-5`*, —* `effort: max`*, —* `memory: project` *— maximum reasoning depth, reserved for architecture and expensive-if-wrong calls. Fable draws usage credits — **(`~2×-Opus`)** — credits, not plan limits, after the ~June-22 launch window. Hence: deep water only.*

      - *— The wheel obeys the coast: subagents cannot spawn subagents, so all routing stays with the orchestrator; the two —* `description` *— fields are the auto-delegation surface. The fleet papers sit on the quarterdeck —* [`../quarterdeck/dispatch.md`](../quarterdeck/dispatch.md) *— section —* `Dispatch-Tier`*.*

---

## (`Three-Deviations-From-The-Commission`/`·`/`Named`/`Not-hidden`)

- The bounty assumed three paths this base does not keep. Conformed to the chambers; logged here:

- **`Charter.md` → `README.md`.** No Charter exists; governance is declared once in
  [`../README.md`](../README.md) (per the MANIFEST). The agents cite README as authoritative.
- **`Fleet.md` → `quarterdeck/dispatch.md`.** No root Fleet chamber; dispatch doctrine lives on the
  quarterdeck. The `Dispatch-Tier` section was appended there, not to a seventh root file — the
  MANIFEST keeps exactly six chambers.
- **`Logbook.md` → `logbook/`.** The logbook is this directory of numbered entries. The agents' running
  one-line tally lives at `dispatch-log.md` (created on first use); retrospective-weight rulings land as
  numbered entries like this one.

Frontmatter itself needed **no** deviation: the sub-agents docs confirm `model` accepts a full id,
`effort` accepts `high`/`max` and overrides session effort, and `memory`/`color` are valid fields.

## (`What-Remains-Honest`/`·`/`residual`/`named-not-hidden`)

- *— Six crew now ride Fable: the prior dispatch-lanes wiring promoted oracle · tessara · the-savant · turbolina-stringweaver · markove-l-hessenanessa, and sailing-master joins them. Fable draws credits at ~2×, credits-not-limits after ~June 22 — the Admiral may want to thin that count before the window closes.*

  - *—* `effort: max` *— overrides session effort (docs-confirmed); whether the quartermaster's —* `high` *— measurably deepens over inherited session effort was pinned by creed, not re-measured live.*

    - *— Registration loads at session start: the fresh-session dispatch + escalation acceptance checks are the Admiral's to run from the panel — an agent cannot restart its own host.*

---

*SID: CLAUDEBASE_LOGBOOK_V1 · Entry 02 · live · 2026-06-10*
