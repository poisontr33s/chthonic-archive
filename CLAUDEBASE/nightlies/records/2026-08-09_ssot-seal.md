---
schema_version: 3
date: 2026-08-09
run_id: 2026-08-09_ssot-seal
trigger: user-invoked
mode: default
lane: ssot-seal
atlas_source: "§1 AxiomVerifier up-cycle entry — surfaced by §1's verify-before-acting step, not listed as a task"
outcome: shipped
verification: pass
commits:
  - hash: 44789449
    kind: feature
landing_doc: claude/mailbox/SESSION_2026_08_09_SSOT_SEAL_AUTONOMOUS_NIGHT.md
self_improvement:
  found: true
  summary: "§1 read as choose-then-verify, but every logged run finds something on the verify step and tonight it was the whole task; §1 now admits a verify-step regression as a valid pick, with live-state confirmation and honest atlas_source attribution"
duration:
  start: 2026-08-09T03:38:58Z
  end: 2026-08-09T03:45:31Z
  elapsed: 6m 33s
---

Atlas §1's AxiomVerifier entry is accurate about what shipped — the seal hash moved
from a `main.rs` literal to the sidecar `.chthonic/SSOT.md.sha256`. Verifying that
claim against live state found the mechanism no longer working: the seal file is
deleted in the working tree, and `verifier.rs:61-67` treats a missing seal as
`Ok(())`, so drift detection is off rather than passing. Independently of the
deletion, the seal committed in HEAD (`1d484686…`) does not match `SSOT.md` in that
same HEAD (`6808095b…`) — sealed by `3f19e50a` on 2026-07-07, canon moved again in
`fadba2e4` on 2026-07-08, never resealed. Stale in-repo for a month.

Landed `ci/checks/ssot-seal.ts` + registry entry: explicit canon list (globbing
`*.sha256` would reproduce the bug — delete the seal, check nothing, go green),
read-only by default with `--strict` to gate, `no_auto_fix: semantic` because
auto-resealing would rubber-stamp the drift it exists to catch. Both branches
exercised against real data; the drift branch required temporarily restoring and
re-deleting the seal, with before/after tree state confirmed identical.

Not decided here: whether to reseal (declaring current content canonical is an
authorship call), whether to restore the deletion (it sits inside a 53-file
working tree from the user's own session), and whether to promote to `--strict`.

Two ledger-shape notes. The commit carrying this record cannot list its own hash,
so `commits` holds only the feature commit — the same friction the README's v2→v3
note records for `zombie-c1`, left as-is rather than papering over. And commit
`44789449` is **local only**: the post-commit push hook failed on a pre-existing
broken link in `CLAUDEBASE/dev/null/salt-trial/AHA_MANIFEST.md:38`, a staged-add
from the user's own session in an authorship domain this skill does not edit. The
commit is real and verified; the push waits on the user.

**Correction appended 2026-08-09, later the same night** (record body corrected
rather than rewritten; the paragraph above states what was true when written).
`44789449` is no longer local — the AHA back door was repaired in
`CLAUDEBASE/dev/null/salt-trial/AHA_MANIFEST.md:38` after the user explicitly
opened that authorship domain, pathfinder went green ("OK: 13 files scanned, all
links valid"), and `45763118..a9f1e88c` pushed all eight commits. The blocker
described above was real and is now closed.

Feature/meta ratio: one feature commit, plus the §7 `SKILL.md` fix and these
documents. Meta stayed the exception. Against `MATURITY.md`'s 7/10 gate — "3-5 more
real runs with NO new major skill-design in between" — this run adds one real run
and deliberately added no new mechanism: the §1 change is a clarification of
existing behavior the ledger already evidenced, not a new mode, flag, or subsystem.
Score left at 7/10; that is the user's to move, and one run does not move it.
