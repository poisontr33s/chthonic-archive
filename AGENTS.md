# (`AGENTS.md`)

## (`Common-Grounds-Meeting-Point`)

- *—* `chthonic-archive/CLAUDEBASE/` *— is the primary workspace for all — **(`Agents.md`/`CLAUDE.md`/`gemini.md`/`Agents.md`/`Codex.md`/`Cline.md`/`Et-Cetera`)**.*

  - *— All agency is within this spesific directory-tree; whether, context, memory, state, management, planning, etc; — **(`Root-DIR`/`Is`/`Forbidden`/`Grounds`)** — serving only as routing, to the **(`Canon-Directory`)** — as per **(`User-Directives`)** — ...nothing to see here.. tag over — **(`@`/`chthonic-archive`/`CLAUDEBASE`)**.*

    - *—* `Agents` *— mustn't write to —* `.gemini/` *— or —* `.claude/` *—* `etc/` *— unless explicitly consolidated..*

      - *— Follow the white rabbit — **(`@`/`Root-DIR`/`chthonic-archive/CLAUDEBASE/`)**.*

        - *—* `~/.chthonic/api_pool.json` *— is the source of truth for API, MCP tokens.*

          - *— Never ask the user to regenerate, refresh, flip, rotate, or paste tokens until —* `.\scripts\api_pool.ps1 -Doctor` *— and the relevant verifier prove the pool token itself is expired/revoked.*

            -  *— Run —* `.\scripts\api_pool.ps1 -Mock` *— and —* `pwsh -NoProfile -File scripts/mcp_write_local.ps1 -Mock` *— before live/provider use when checking wiring.*

              - *— Use —* `.\scripts\api_pool.ps1 -Load` *— for current-process env.*

                - — *Use —* `pwsh -NoProfile -File scripts/mcp_write_local.ps1 -GitHubMode copilot` *— to restore `.mcp.json` after IDE/config updates.*

                  - *— Use —* `pwsh -NoProfile -File scripts/mcp_write_local.ps1 -List` *— to report MCP servers without printing secrets.*

                    - *— Token renewal is manual only; scripts must not mint, refresh, rotate, or regenerate tokens.*
---

## (`Claude`/`Gemini`/`Codex`/`Cline`/`Et.Cetera`/`Bridge-Immutables`)

- *— **(`Truth-Surface`)** — all state checkpoints, memory files, and trajectories MUST be written losslessly to **(`CLAUDEBASE`)**-*

  - *— **(`Continuity`)** — treat it as  absolute primary context.*

---
