Explore the chthonic-archive corpus pipeline to give me a precise map of the current state at G5 (commit f7f276a9). I need this for planning G6–G9 (recency scoring, semantic embeddings, entity graph, temporal facts).

Focus on these three TypeScript files in the repo root or scripts/ directory (find them):
1. `session-watcher.ts` — FS watcher mirroring VSCode Copilot Chat sessions
2. `session-corpus.ts` — SQLite ingestor (WAL mode, FTS5 indices)
3. `corpus-mcp.ts` — Bun stdio MCP server exposing 15 tools

For each file, report:
- Exact file path
- Approximate line count
- Top-level structure: what classes/functions/handlers are exported or registered
- Database connection pattern: how `openDB()` / `openDBWrite()` are defined and used (per-call open/close vs persistent)
- For corpus-mcp.ts specifically: enumerate ALL 15 MCP tools by name, their input schema shape, and what they return. Also list the MCP Resources registration (corpus://session/<id>).
- Note any existing helpers for: chunking text, computing scores, joining FTS5 to base tables, JSON serialization patterns

Also check:
- `corpus.sqlite` schema (read from the CREATE TABLE / CREATE VIRTUAL TABLE statements in session-corpus.ts)
- `package.json` — what's installed (especially @modelcontextprotocol/sdk version, bun:sqlite usage, any embedding/vector libraries already pulled in)
- `.mcp.json` at the project root — how corpus-mcp is registered
- Any existing references to: `sqlite-vec`, `nomic-embed`, `ollama`, `embedding`, `graphiti`, or `recency` in the codebase

Report back as structured prose with file:line citations. Cap at ~500 words. Do NOT propose designs — I just need the ground truth of what exists.