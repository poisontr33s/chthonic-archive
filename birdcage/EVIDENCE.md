---
type: ledger
category: aai-evidence
version: "1.0"
created: "2026-05-11T03:48:13.065Z"
ticket: "5000b0e1"
wptg: true
---

# Birdcage — AAI Evidence Ledger

> **Verdict:** Birdcage v0.0.1 is a viable POC. The auth and routing chain is *proved*. The budget gate is not a failure — it is a boundary condition that proves auth works.

---

## Evidence Summary

| Probe | Surface | Outcome | Auth Chain | Notes |
|-------|---------|---------|-----------|-------|
| C | `vscode-insiders://copilot-chat` | ✅ `BIRDCAGE-OK` | Manual | 2× green runs |
| A | `azure-for-github` (models.github.ai) | 🟡 Budget-gated | ✅ Reached | Route works; account at limit |
| B | Foundry Local (localhost:5273) | Not run | N/A | Requires local Foundry model pull |

---

## Run Log (raw `runs.jsonl` decoded)

### Run 1 — Probe C — 2026-05-06T02:57:07Z
- Surface: `vscode-insiders://copilot-chat`  
- OK: `true`  
- Response: `BIRDCAGE-OK`  
- Method: Manual (paste)  

### Run 2 — Probe C — 2026-05-06T03:18:41Z
- Surface: `vscode-insiders://copilot-chat`  
- OK: `true`  
- Response: `BIRDCAGE-OK`  
- Method: Manual (paste)  

### Run 3 — Probe A — 2026-05-06T03:20:56Z
- Surface: `azure-for-github` / `openai/gpt-4o-mini`  
- OK: `false`  
- Error class: `PermissionDeniedError` (HTTP 403)  
- Error: `Unable to proceed with model usage. This account has reached its budget limit.`  
- Stage: `provider_policy`  

### Run 4 — Probe A (structured re-run) — 2026-05-06T03:25:42Z
- Surface: `azure-for-github` / `openai/gpt-4o-mini`  
- OK: `false`  
- Auth reached: **`true`** — the token auth chain is valid  
- Budget gated: **`true`** — the GHCP Pro+ account budget is exhausted  
- Route status: `budget_gated`  

---

## Interpretation

### What "budget gated" proves

`auth_reached: true` in the structured record is positive evidence. The request:
1. Formed a valid OpenAI-compatible payload
2. Resolved `models.github.ai/inference` via DNS
3. Transmitted the GHCP Pro+ bearer token
4. Was accepted by the auth layer (no 401)
5. Reached the provider policy layer (budget check fires *after* auth)

The 403 is a **quota boundary**, not a routing or auth failure. The cage door opened — the bird just hit the budget wall on the far side.

### What this means for AAI readiness

- **Interactive surface (Probe C):** Green. Claude 4.x / GPT-4o in Copilot Chat is an active, proved surface.  
- **API surface (Probe A):** Route is valid. Unblock = add billing credits or switch to a free-tier model endpoint.  
- **Local surface (Probe B):** Not yet trialed. Requires pulling a Foundry-compatible model to `localhost:5273`.  

---

## Version Pins (at evidence time)

| Component | Version |
|-----------|---------|
| Python | ≥3.14 (requires-python in pyproject.toml) |
| openai SDK | ≥1.50.0 |
| httpx | ≥0.27.0 |
| python-dotenv | ≥1.0.1 |
| Model (Probe A) | `openai/gpt-4o-mini` via `models.github.ai` |
| Model (Probe B) | `phi-3.5-mini-instruct-cuda-gpu` (local Foundry) |
| Birdcage version | 0.0.1 |

---

## Python 3.14 Ecosystem Constraints

The birdcage pyproject.toml requires `python>=3.14`. At the time of evidence collection (2026-05-06):

- `openai`, `httpx`, `python-dotenv` all have cp314 wheels — birdcage's core deps are clear.
- `flash_attn`, `exllamav2` — no cp314 wheels (documented separately in tabbyAPI gate ladder).
- The birdcage deps are lightweight enough that py314 is viable for this surface.

---

## Next Actions to Unblock Probe A

1. **Add GHCP Pro+ billing credits** — cheapest path, same token, same model.
2. **Switch to HF Inference API** — free tier with rate limits (no budget gate).
3. **Switch to Poe API** — account lanes already configured in `scripts/chthonic.ps1 poe`.
4. **Foundry local (Probe B)** — zero cost, requires model pull, GPU-bounded.

---

## WPTG Classification

| Item | Classification | Action |
|------|---------------|--------|
| `runs.jsonl` | Canonical signal — raw observability | Preserve, append on re-runs |
| `probes.py` | Canonical signal — probe harness | Extend; do not delete |
| `cage.toml` | Canonical signal — scope walls | Extend; budget-gate is a scope constraint to document |
| This file | Ledger artifact — AAI evidence | SSOT for Birdcage probe state |

---

*Ticket: `5000b0e1` · Generated by Claudine autonomous lane · 2026-05-11*
