# Copy to .env and fill in. .env is gitignored.  ( Fresh TOKEN exists in  chthonic API pool script **updated**)

# GHCP Pro+ token used for Azure-for-GitHub models endpoint (Probe A).
# Generate / view at: https://github.com/settings/tokens  (or your org SSO flow) (Depending on Token flipping or save time not flipping the infra already working) 
GITHUB_TOKEN=

# Optional overrides — leave blank to use cage.toml defaults.
FOUNDRY_BASE_URL=
FOUNDRY_MODEL=
